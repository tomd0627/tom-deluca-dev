---
export interface Props {
  label: string;
  type: "button" | "submit" | "reset";
}

const { label, type } = Astro.props;
---

<div class="form-field animate-fade">
  <button class="button form-input form-input--button" type={type}
    >{label}</button
  >
</div>
